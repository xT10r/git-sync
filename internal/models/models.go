package models

/*
package models

import (
	"context"
	"time"
)

type MetricItem struct {
	Name      string    `json:"name"`      // Имя метрики (Наименование метрики в виде текста?)
	Value     float64   `json:"value"`     // Значение метрики в виде числа с плавающей точкой
	Timestamp time.Time `json:"timestamp"` // Момент времени, когда была получена метрика
	Id        int64     `json:"id"`        // Идентификатор записи в хранилище, для простоты пусть будет в виде целого числа
}

type RepositoryWriteInterface interface {
	Add(ctx context.Context, item *MetricItem) (MetricItem, error)
}

type RepositoryReadInterface interface {
	ListMetrics(ctx context.Context, name string, from, to time.Time) ([]MetricItem, error)
	// TODO: задание 8+. Доделать после сдачи заданий 6+
	// ListChecks(ctx context.Context, domain string, num int64) ([]MetricCheck, error)
}
*/
